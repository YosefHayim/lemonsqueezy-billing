# Context Awareness

## When Starting a Task

1. **Project detection:** Infer project type from `package.json`, `tsconfig.json`, lockfiles, etc. Build context from the detected setup.
2. **Date and branch:** Note today's date and the current git branch when relevant (changelogs, PRs, deployment).

Keep these in mind; they help provide accurate, context-aware responses.
